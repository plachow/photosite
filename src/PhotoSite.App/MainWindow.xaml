<Window x:Class="PhotoSite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:PhotoSite.Controls"
        xmlns:viewModels="clr-namespace:PhotoSite.ViewModels"
        Title="PhotoSite"
        Icon="{StaticResource AppIcon}"
        Width="1500"
        Height="900"
        MinWidth="1200"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="OnPreviewKeyDown">
  <Window.InputBindings>
    <KeyBinding Key="Left" Command="{Binding PreviousCommand}" />
    <KeyBinding Key="Right" Command="{Binding NextCommand}" />
  </Window.InputBindings>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition>
        <RowDefinition.Style>
          <Style TargetType="RowDefinition">
            <Setter Property="Height" Value="Auto" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsEditorMode}" Value="True">
                <Setter Property="Height" Value="0" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </RowDefinition.Style>
      </RowDefinition>
      <RowDefinition Height="*" />
      <RowDefinition>
        <RowDefinition.Style>
          <Style TargetType="RowDefinition">
            <Setter Property="Height" Value="Auto" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsEditorMode}" Value="True">
                <Setter Property="Height" Value="0" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </RowDefinition.Style>
      </RowDefinition>
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
            Background="{StaticResource PanelBrush}"
            BorderBrush="#303541"
            BorderThickness="0,0,0,1"
            Padding="8">
      <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Left"
                Content="Open folder"
                Command="{Binding OpenFolderCommand}" />
        <ProgressBar DockPanel.Dock="Right"
                     Width="90"
                     Height="4"
                     Margin="12,0,4,0"
                     IsIndeterminate="{Binding IsBusy}" />
        <TextBlock Margin="10,0"
                   VerticalAlignment="Center"
                   Foreground="#C8CDD8"
                   TextTrimming="CharacterEllipsis"
                   Text="{Binding CurrentFolder, FallbackValue=No folder selected}" />
      </DockPanel>
    </Border>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="NavigatorColumn"
                          Width="260"
                          MinWidth="180" />
        <ColumnDefinition x:Name="NavigatorSplitterColumn" Width="5" />
        <ColumnDefinition x:Name="CatalogColumn"
                          Width="420"
                          MinWidth="280" />
        <ColumnDefinition x:Name="CatalogSplitterColumn" Width="5" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Grid Grid.Column="0" Background="{StaticResource PanelBrush}">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border BorderBrush="#303541"
                BorderThickness="0,0,0,1"
                Padding="5,4">
          <CheckBox Content="Include subfolders"
                    IsChecked="{Binding IncludeSubfolders, Mode=TwoWay}"
                    ToolTip="Scan the selected folder recursively" />
        </Border>

        <TreeView Grid.Row="1"
                  Padding="4,8"
                  Background="{StaticResource PanelBrush}"
                  BorderThickness="0"
                  Foreground="#E4E7ED"
                  ItemsSource="{Binding DirectoryTree.Roots}"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
        <TreeView.Resources>
          <HierarchicalDataTemplate DataType="{x:Type viewModels:DirectoryNodeViewModel}"
                                    ItemsSource="{Binding Children}">
            <Grid Margin="2,3">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="22" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0"
                         VerticalAlignment="Center"
                         FontFamily="Segoe UI Symbol, Segoe UI Emoji"
                         Foreground="#A9CFF2"
                         Text="{Binding Glyph}" />
              <TextBlock Grid.Column="1"
                         VerticalAlignment="Center"
                         Text="{Binding DisplayName}"
                         ToolTip="{Binding FullPath}" />
              <TextBlock Grid.Column="2"
                         Margin="6,0,0,0"
                         VerticalAlignment="Center"
                         Foreground="#79808D"
                         Text="…">
                <TextBlock.Style>
                  <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding IsLoading}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </TextBlock.Style>
              </TextBlock>
            </Grid>
          </HierarchicalDataTemplate>
        </TreeView.Resources>
        <TreeView.ItemContainerStyle>
          <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="#E4E7ED" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="IsExpanded"
                    Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected, Mode=TwoWay}" />
            <Style.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#284967" />
                <Setter Property="Foreground" Value="White" />
              </Trigger>
              <DataTrigger Binding="{Binding IsPlaceholder}" Value="True">
                <Setter Property="Opacity" Value="0.55" />
                <Setter Property="IsHitTestVisible" Value="False" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TreeView.ItemContainerStyle>
        </TreeView>
      </Grid>

      <GridSplitter Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Background="#303541"
                    DragCompleted="OnPaneSplitterDragCompleted" />

      <ListBox x:Name="PhotoList"
               Grid.Column="2"
               Background="{StaticResource PanelBrush}"
               BorderThickness="0"
               ItemsSource="{Binding Photos}"
               SelectedItem="{Binding SelectedPhoto}"
               MouseDoubleClick="OnPhotoListMouseDoubleClick"
               ScrollViewer.CanContentScroll="True"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               ScrollViewer.VerticalScrollBarVisibility="Auto"
               VirtualizingPanel.IsVirtualizing="True"
               VirtualizingPanel.VirtualizationMode="Recycling">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <controls:VirtualizingTilePanel ItemWidth="204" ItemHeight="164" />
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="{StaticResource RaisedBrush}" />
            <Setter Property="BorderBrush" Value="#333844" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                <Setter Property="BorderThickness" Value="2" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="116" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <Border Background="#0C0E12" ClipToBounds="True">
                <controls:ThumbnailPresenter SourcePath="{Binding Path}"
                                             Stretch="UniformToFill" />
              </Border>
              <Grid Grid.Row="1" Margin="8,5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock FontSize="12"
                             Text="{Binding FileName}"
                             TextTrimming="CharacterEllipsis"
                             ToolTip="{Binding Path}" />
                  <TextBlock FontSize="10"
                             Foreground="#9299A7"
                             Text="{Binding SizeText}" />
                </StackPanel>
                <TextBlock Grid.Column="1"
                           Margin="8,0,0,0"
                           Foreground="#FFD36A"
                           Text="{Binding Rating, StringFormat=★ {0}}" />
              </Grid>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <GridSplitter Grid.Column="3"
                    HorizontalAlignment="Stretch"
                    Background="#303541"
                    DragCompleted="OnPaneSplitterDragCompleted" />

      <Grid Grid.Column="4">
        <Grid.RowDefinitions>
          <RowDefinition>
            <RowDefinition.Style>
              <Style TargetType="RowDefinition">
                <Setter Property="Height" Value="0" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsEditorMode}" Value="True">
                    <Setter Property="Height" Value="Auto" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </RowDefinition.Style>
          </RowDefinition>
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="#303541"
                BorderThickness="0,0,0,1"
                Padding="5">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="◀" ToolTip="Previous" Command="{Binding PreviousCommand}" />
            <Button Content="▶" ToolTip="Next" Command="{Binding NextCommand}" />
            <Separator Margin="8,2" />
            <Button Content="↶"
                    ToolTip="Rotate left"
                    Command="{Binding SelectedPhoto.RotateLeftCommand}" />
            <Button Content="↷"
                    ToolTip="Rotate right"
                    Command="{Binding SelectedPhoto.RotateRightCommand}" />
            <Button Content="⇋"
                    ToolTip="Flip horizontally"
                    Command="{Binding SelectedPhoto.FlipHorizontalCommand}" />
            <Button Content="Reset"
                    Command="{Binding SelectedPhoto.ResetEditsCommand}" />
            <Separator Margin="8,2" />
            <TextBlock Margin="5,0"
                       VerticalAlignment="Center"
                       Foreground="#C8CDD8"
                       Text="Rating" />
            <ComboBox x:Name="RatingComboBox"
                      Width="60"
                      ItemsSource="{Binding RatingValues}"
                      SelectedItem="{Binding SelectedPhoto.Rating}" />
          </StackPanel>
        </Border>

        <controls:PhotoViewer x:Name="PreviewViewer"
                              Grid.Row="1"
                              SourcePath="{Binding SelectedPhoto.Path}"
                              EditRecipe="{Binding SelectedPhoto.EditRecipe}"
                              IsEditorMode="{Binding IsEditorMode}"
                              PreviousPhotoCommand="{Binding PreviousCommand}"
                              NextPhotoCommand="{Binding NextCommand}"
                              DoubleClickCommand="{Binding ShowManagerCommand}" />
      </Grid>
    </Grid>

    <Border Grid.Row="2"
            Background="{StaticResource PanelBrush}"
            BorderBrush="#303541"
            BorderThickness="0,1,0,0"
            Padding="10,5">
      <DockPanel>
        <TextBlock DockPanel.Dock="Right"
                   Foreground="#9299A7"
                   Text="Wheel: zoom · drag: pan · Home: fit · thumbnail double-click / Enter: Editor" />
        <TextBlock Foreground="#C8CDD8" Text="{Binding StatusText}" />
      </DockPanel>
    </Border>
  </Grid>
</Window>
